#include <stdio.h>
#include <math.h>
int main()
{
    // int a = 4, b, c;
    // b = c = a; // Type Delacration Intruction
    // printf("The value of a is %d \n", a);
    // printf("The value of b is %d \n", b);
    // printf("The value of c is %d \n", c);

    int a = 4; // Arithmeic Intruction
    int b = 8;
    // printf("The Value of a + b is %d \n", a + b);
    // printf("The Value of a - b is %d \n", a - b);
    // printf("The Value of a * b is %d \n", a * b);
    // printf("The Value of a / b is %d \n", a / b);
    // printf("5 when divided by 2 leaves a remainder of %d\n", 5 % 2);
    // printf("-5 when divided by 2 leaves a remainder of %d\n", -5 % 2);
    // printf("5 when divided by -2 leaves a remainder of %d\n", -5 % (-2));
    // printf("5 when divided by -2 leaves a remainder of %d\n", -5 % -2);
    printf("The Value of 4 to the power of 5 is %f\n", pow(2, 5));

    // 😀 Type Coversion
    // int and int = int
    // int and float = float
    // float and float = float

    printf("The value of %f\n ", 5 + 6.5);
    printf("The value of %f\n ", 5 + 5);
    printf("The value of %f\n ", 5.6 + 6.5);
    return 0;
}